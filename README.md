# Store Sales Analysis

Небольшой учебный проект, демонстрирующий базовую работу с
**Pandas** и **NumPy** на примере анализа продаж магазина.
В проекте также используются **Matplotlib** для визуализации.

## 🎯 Цель проекта
- Научиться загружать и очищать данные
- Выполнять базовый анализ данных
- Использовать NumPy для расчёта статистики
- Строить графики с помощью Matplotlib

## 🧰 Используемые библиотеки
- pandas
- numpy
- matplotlib

## 📁 Структура проекта
```
store-sales-analysis/
├── data/
│ ├── sales.csv # исходные данные
│ └── cleaned_sales.csv # очищенные данные
├── plots/
│ ├── revenue_by_category.png
│ └── top_5_products.png
├── main.py
├── requirements.txt
└── README.md
```

## 📊 Описание данных
Файл `sales.csv` содержит информацию о продажах:

- `date` — дата продажи
- `product` — название товара
- `category` — категория товара
- `price` — цена за единицу
- `quantity` — количество
- `city` — город продажи

## 🔍 Что делает проект
1. Загружает данные из CSV
2. Приводит даты к правильному формату
3. Обрабатывает пропущенные значения:
   - `price` заполняется медианой
   - `quantity` заполняется единицей
   - `city` заполняется "Unknown"
4. Удаляет дубликаты
5. Рассчитывает выручку (`revenue = price * quantity`)
6. Выполняет базовый анализ:
   - общая выручка
   - выручка по категориям
   - топ товаров по количеству продаж
7. Рассчитывает статистику с помощью NumPy:
   - среднее
   - медиана
   - стандартное отклонение
8. Строит графики с помощью Matplotlib и сохраняет их в `plots/`
   - `revenue_by_category.png`
   - `top_5_products.png`
9. Сохраняет очищенные данные в `data/cleaned_sales.csv`

## 📈 Визуализация
- Выручка по категориям — `plots/revenue_by_category.png`
- Топ-5 товаров по количеству — `plots/top_5_products.png`

## 🚀 Как запустить
```bash
pip install -r requirements.txt
python main.py
```
